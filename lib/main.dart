import 'package:flutter/material.dart';

//
void main() => {
      runApp(
        //MaterialAppが下のものを持ってる
        const MaterialApp(
          home: Center(
            child: Text('git hub test'),
          ),
        ),
      ),
    };
